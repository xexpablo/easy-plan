import 'package:flutter/material.dart';
import 'package:testando/components/Background.dart';

class LoginBox extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: <Widget>[
          TextFormField(),
        ],
      ),
    );
  }
}
